# Known Limitations

- The package targets the static flagship only.
- The forced-answer witnesses are empirical witnesses plus a structural graph obstruction, not a census of all algorithms.
- Nuisance split controls use random binary splitters with the frozen seed protocol.
- The data release is a frozen audit table release, not a raw DNS trajectory archive.
